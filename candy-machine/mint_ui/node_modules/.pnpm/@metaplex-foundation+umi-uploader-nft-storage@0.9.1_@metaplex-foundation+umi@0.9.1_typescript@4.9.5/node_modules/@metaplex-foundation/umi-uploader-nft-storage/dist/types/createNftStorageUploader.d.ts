import { Context, Signer, UploaderInterface } from '@metaplex-foundation/umi';
import { NFTStorageMetaplexor } from '@nftstorage/metaplex-auth';
import { NFTStorage } from 'nft.storage';
export type NftStorageUploaderOptions = {
    payer?: Signer;
    token: string;
    endpoint?: URL;
    gatewayHost?: string;
    batchSize?: number;
    useGatewayUrls?: boolean;
};
export declare function createNftStorageUploader(context: Pick<Context, 'rpc' | 'payer'>, options?: NftStorageUploaderOptions): UploaderInterface & {
    client: () => Promise<NFTStorage | NFTStorageMetaplexor>;
};
